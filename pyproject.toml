[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "influ-match"
version = "0.1.0"
description = "Influence-guided relational consistency plugin for USB"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools]
packages = ["influmatch"]
